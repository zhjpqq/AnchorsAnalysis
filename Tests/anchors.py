from torch.autograd import Variable
from torch.autograd import Variable as V
import torch as t
import torch
import numpy as np

image_shape = np.array([512, 512, 3])
fmap_shape = np.array([256, 256])
scales = np.array([16, 32, 64, 128])
ratios = np.array([0.5, 1, 2])
zero_area = 1








